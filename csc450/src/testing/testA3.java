package testing;

public class testA3 {
    // We define a function for the ground
    // Arc of the mortor
    // Z = H(X,Y) determines the position of the mortar
    // We have a vector (X, Y, H(X,Y))
}
